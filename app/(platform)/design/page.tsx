import DesignStudioHero from "@/components/DesignStudioHero";

// The Design Studio renders as a full immersive workspace matching the
// reference — viewport, KPIs, AI assistant, options generator, review
// workflow, and live analysis. The parametric drawing tools (DrawingGenerator,
// RebarDetail) remain in the component library and are wired behind the
// Plans / Materials viewport tabs.
export default function DesignPage() {
  return <DesignStudioHero />;
}
